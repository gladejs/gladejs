name: CI

on:
    push:
        branches:
            - win-path-fix

jobs:
    testing:
        runs-on: windows-latest

        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v2
              with:
                  node-version: 16.x
            - run: npm install
            - run: npm run build
